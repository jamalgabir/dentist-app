import React from 'react';
import BookingForm from '../components/BookingForm';

const Booking = () => {
    return (
        <section>
            <BookingForm />
        </section>
    );
};

export default Booking;